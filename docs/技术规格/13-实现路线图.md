# Coding Agent 技术规格 · 13 实现路线图

> 状态：**已确认 v0.2**（2026-08-14）｜里程碑：阶段 7（设计定稿 → 实现）｜前置：`00`-`12` 全部确认
> 定位：把 13 份设计规格转成可执行的开发计划。**不重复设计内容**——每里程碑锚定对应规格的 §5 验收标准，做到什么算完直接勾选。

---

## 1. 目标与范围

**本文件回答：从零代码到五档全部落地，怎么走？**

- **里程碑拆分**：五档路线 → M0-M5，每里程碑定义内容 / 对应规格 / 验收锚点 / 关键预研 / 建议节奏。
- **依赖顺序**：M0 → M1 → M2 → M3 → M4 → M5 线性推进（里程碑内部允许并行子任务）。
- **横切机制**：测试闭环的最小形态（pytest 覆盖核心）从 **M1** 引入，完整 `TestRunner`/worktree 沙箱到 **M5**（D5：方案四+三捆绑优先——`07`/`11` 数据层 M3 落地、`12` 闭环 M5 整合）。
- **量化改进**：M1-M4 靠 pytest + 手动 demo 验收；跨版本改进（prompt/超参/harness）由 `11` 一次一变量复测度量（M5 后）。

**不在本文件范围**：设计细节（见 `00`-`12`）、具体排日排周（学习项目给相对节奏）。

---

## 2. 里程碑总览

| 里程碑 | 名称 | 档位 | 核心内容 | 对应规格 | 前置 |
|---|---|---|---|---|---|
| M0 | 工程骨架 | — | 包结构 + 工具链 + 冒烟测试 | 00 §1 | — |
| M1 | 能跑最小闭环 | 能跑 | 引擎 + 7 工具 + 会话 + TUI 基础 | `02` `03` `04` `05` | M0 |
| M2 | 能用 | 能用 | 三层压缩 + 结构化日志 + `/compact` | `01` `07` `04` | M1 |
| M3 | 可控 | 可控 | 权限五层 + Hook + trace/监控 | `06` `07` `05` | M2 |
| M4 | 好用 | 好用 | 静默记忆 + MCP/Skill | `08` `09` `05` | M3 |
| M5 | 生产级 | 生产级 | SubAgent/Worktree + 评估 + Harness | `10` `11` `12` | M4 |

**建议节奏**：每里程碑 1-3 周（学习项目，按每日投入浮动）。里程碑完成 = 验收锚点勾选全绿 + 可演示。

---

## 3. 里程碑明细

### 3.0 子任务卡（实现期工作流）

每个子任务（M1-a、M2-b …）开工前按此模板填写。它是实现期的**任务契约**——OpenSpec changeset 的瘦身版：不引入额外工具、不单建文件，直接写进本文件各里程碑小节，避免文档膨胀。

| 字段 | 说明 |
|---|---|
| 目标 | 一句话：本子任务交付什么 |
| 判据 | 可验证断言：锚定对应规格 §5 的具体项；规格未覆盖处自拟断言 |
| 产出 | 文件清单 + 冒烟测试（pytest） |
| 依赖 | 前置子任务（若有） |
| 可跳过 | 是 / 否 + 理由（依赖后续里程碑回填的标"否"） |

> **判据 = 完成定义**：勾选即算完成，与 `12` 的"完成 = 判据被满足"同一把尺子。
> 每个子任务完成即在该里程碑小节内联勾选 `[x]`，里程碑结束时汇总到 §5 验收机制（规格 §5 全绿 + 可演示）。

### M0 工程骨架（<1 周）

| 项 | 内容 | 默认选型（可改） |
|---|---|---|
| 包结构 | `src/kdagent/`，按规格建目录 + `__init__.py` + 核心类型/协议占位 | 各目录见下 |
| 工具链 | `ruff`（lint+format）、`mypy`（严格）、`pytest` + `pytest-asyncio` | 三份配置 + Makefile 或 `uv run` 封装 |
| 依赖声明 | `pyproject.toml` + `uv.lock`（或 requirements.txt） | `uv`（快）+ Textual + httpx + 官方 `mcp` 包 |
| 冒烟测试 | `pytest` 跑通 import + 最小断言 | `tests/test_smoke.py` |

**目录映射**（对应规格模块）：

```
src/kdagent/
  engine/       # 02 核心引擎（llm/ 对话/ agent loop）
  tools/        # 03 工具系统（base/ registry/ 内置7个）
  sessions/     # 04 会话管理
  context/      # 01 上下文工程（history/ compactor/ tool_result_handler）
  ui/           # 05 Textual TUI（chat/ todo/ confirm/ commands）
  permission/   # 06 权限（checker/ rules）
  hooks/        # 06 Hook（engine）
  obs/          # 07 可观测性（telemetry/ exporters/ query）
  memory/       # 08 记忆（extractor/ consolidate）
  mcp/ skill/   # 09 工具生态（mcp client wrapper / skill loader）
  subagent/     # 10 SubAgent + Worktree
  eval/         # 11 评估 driver
  harness/      # 12 测试闭环 + 检查点 + 规则追踪
  config.py     # 配置加载（用户级/项目级/本地级三源合并）
```

**验收锚点**（2026-08-14 已通过 ✅）：`uv run pytest` 绿（3 passed）；`uv run mypy src` 干净（19 文件）；`uv run ruff check` 干净；`uv run kdagent --version` → `kdagent 0.1.0`。

**M0 内部要定的选型**（本阶段确认，不改设计）：包名 `kdagent`、依赖管理器 `uv`、Python ≥3.11（开发机实测版本）。

### M1 能跑最小闭环（能跑档 · 主价值）

**目标**：`02`+`03`+`04`+`05` 最小闭环——DeepSeek（OpenAI 兼容 adapter）跑通 ReAct Loop，7 个内置工具可用，TUI 能对话、能 /session 存档读档。

**示例卡：M1-a** ✅ 已完成（2026-08-14：pytest 33 / mypy strict / ruff clean / 真实 DeepSeek 流式通过）（其余子任务照此内联填写，M2-M5 同）

| 字段 | 填写 |
|---|---|
| 目标 | `02` 消息模型 + provider 抽象（anthropic/openai 两 adapter）+ ConversationManager |
| 判据 | `02` §5 能跑档消息/对话相关项全绿；`mypy strict` 干净 |
| 产出 | `src/kdagent/engine/llm/{protocols,providers}.py`、`engine/conversation.py` + `tests/test_message_model.py` |
| 依赖 | M0 |
| 可跳过 | 否 |

**子任务卡：M1-b** ✅ 已完成（2026-08-14：pytest 64 / mypy strict / ruff clean；Grep 依赖 rg 15.1.0 已装）

| 字段 | 填写 |
|---|---|
| 目标 | `03` 工具协议 + ToolRegistry + 7 内置工具（ReadFile/WriteFile/EditFile/Glob/Grep/Bash/TodoWrite） |
| 判据 | `03` §5 工具系统项：协议元信息（is_read_only/is_destructive/is_concurrency_safe/require_confirm/validate_input）；registry 重名注册 fail fast；`schemas()` 生成 `02` ToolSchema 可直接入 Payload；内置 7 工具齐全；TodoWrite 规范化（todo→task→steps，accept_criteria 可选）；conversation 改用 `tools.base.ToolResult`（含 name/duration_ms） |
| 产出 | `src/kdagent/tools/{base,registry,filesystem,shell,todo}.py` + `tools/__init__.py`（build_default_registry）+ `tests/test_tool{_registry, s_todo, s_filesystem, s_shell}.py` |
| 依赖 | M1-a（复用 `02` ToolSchema / conversation） |
| 可跳过 | 否 |

**子任务卡：M1-c** ✅ 已完成（2026-08-14：pytest 77 含 live / mypy strict / ruff clean；真实 DeepSeek 端到端通过）

| 字段 | 填写 |
|---|---|
| 目标 | `02` ReAct Loop + AgentEvent 事件流 + 分批执行 + 四停止条件 + 断路器 |
| 判据 | `02` §5 相关项：事件流 9 类齐全；单轮/多轮交替合法；工具不存在返回 errorResult 不终止；MAX_ITERATIONS 强制停；取消时已收部分落库；[Read,Read,Edit,Read,Read] 拆 3 批；断路器连续 3 次注入 reminder |
| 产出 | `src/kdagent/engine/{agent,events}.py` + `tests/test_agent_{loop,live,partition}.py` |
| 依赖 | M1-a + M1-b |
| 可跳过 | 否 |

**子任务卡：M1-d** ✅ 已完成（2026-08-14：pytest 92 / mypy strict / ruff clean）

| 字段 | 填写 |
|---|---|
| 目标 | `04` 会话 JSONL 持久化 + 恢复四步 + 过期清理 |
| 判据 | `04` §5 相关项：每条消息一行实时落盘；坏行跳过；恢复映射合法；悬空 tool_use 在出口补 errorResult（Agent 发请求前 repair_chain）；孤儿 tool_result 剔除；>24h 注入时间提示；list 倒序；30 天清理可开关；SessionRecord.todos 序列化（TodoWrite 接线留 M1-f） |
| 产出 | `src/kdagent/sessions/{records,manager}.py` + `tests/test_sessions_{records,manager}.py`；`ConversationManager.restore`；Agent 出口链修复 |
| 依赖 | M1-c |
| 可跳过 | 否 |

**子任务卡：M1-e** ✅ 已完成（2026-08-14：pytest 117 / mypy strict / ruff clean / 真实 DeepSeek TUI 端到端通过）

| 字段 | 填写 |
|---|---|
| 目标 | `05` TUI 三区域 + 事件流渲染 + 极简 Y/N 确认 + Slash 7 命令 |
| 判据 | `05` §5 能跑档项：三区域启动渲染/关闭干净；text_delta 实时渲染 + LoopComplete 滚底；ToolRegion "正在执行 X" + 结果折叠；StatusBar 随 UsageEvent 更新 token；Esc 取消干净可继续；require_confirm 工具弹 Y/N（no → is_error 拒绝结果，Loop 继续）；7 命令可用 + 只输 `/` 列命令；别名冲突启动报错；未知命令带 /help 引导；Tab 补全（/ 列命令、前缀补全、多匹配候选）；真实 DeepSeek TUI 端到端（输入→Agent→渲染→落库） |
| 产出 | `src/kdagent/ui/{app,commands,chat,toolregion,statusbar,confirm}.py`；`cli.py` 启动 TUI（无 key 仍可启动，对话时报错引导）；`ToolContext.confirm` 钩子 + Agent require_confirm 前置 + `set_system_prompt`（/plan 切换）；tests/test_{commands,confirm,ui_app,ui_live}.py |
| 依赖 | M1-c（UI 复用 02 事件流）；会话目录复用 M1-d SessionManager |
| 可跳过 | 否 |

**子任务卡：M1-f** ✅ 已完成（2026-08-14：pytest 136+3 live / mypy strict / ruff clean；真实 DeepSeek TUI 端到端通过）

| 字段 | 填写 |
|---|---|
| 目标 | `04`+`05` 打通：`/session list/resume/new/delete` 全接线；TodoWrite → SessionRecord.todos → TodoRegion 渲染 |
| 判据 | `04` §5 + `05` §5 打通项：`/session` 四子命令可用（new 切换 Agent conversation、resume 恢复消息历史、delete 删文件、list 倒序展示）；TodoWrite 每次调用经 `ToolContext.todos` 回调 → Agent sink → KDApp `_on_todos` → `todo_items_from_raw`（todo→task→steps，task 挂 group=所属 todo 目标）→ `Session.set_todos` → TodoRegion 按 group 分组渲染三层；消息每次 mutation 经 `on_conversation_change` → `session.flush_last()` 实时落盘（todos 随最新记录持久化，resume 恢复） |
| 产出 | `sessions/records.py`（TodoItemRecord 加 group + `todo_items_from_raw`）；`tools/base.py`（`TodosCallback`）；`tools/todo.py`（execute 触发回调）；`engine/agent.py`（todos sink + `set_conversation` + `on_conversation_change`）；`ui/todoregion.py`（TodoRegion）；`ui/commands.py`（session 命令接线）；`ui/app.py`（会话持有 + 回调接线）+ tests/test_{commands,sessions_manager,sessions_records,tools_todo,todo_link,ui_app}.py |
| 依赖 | M1-d（SessionManager / SessionRecord）+ M1-e（UI） |
| 可跳过 | 否 |

**子任务顺序**（按依赖）：

```
M1-a  [x] 02 消息模型 + provider 抽象（anthropic/openai 两 adapter）+ ConversationManager
M1-b  [x] 03 工具协议 + registry + 7 内置工具（ReadFile/WriteFile/EditFile/Glob/Grep/Bash/TodoWrite）
M1-c  [x] 02 ReAct Loop + 事件流 + 分批执行 + 四停止条件
M1-d  [x] 04 会话 JSONL 持久化 + 恢复四步 + 过期清理
M1-e  [x] 05 TUI 三区域 + 事件流渲染 + 极简 Y/N 确认（require_confirm 前置）+ Slash 7 命令
M1-f  [x] 04+05 打通：/session list/resume/new/delete；TodoWrite → SessionRecord.todos → TodoRegion 渲染
M1-g  [x] 03 六要素描述 review（人工，D11）：Glob/TodoWrite/Bash 补缺，验收底稿见 `M1-g-工具描述六要素review.md`
M1-h  [x] 手动验收 17 项反馈修复（2026-08-14）：repair_chain 补悬空 tool_use（400 根因）；ChatInput(TextArea) 解决中文 IME + priority Tab 补全 + 复制粘贴；ChatView markdown 渲染 + escape 防错位；弹窗居中；resume 历史渲染；Claude Code 风布局（todo/tools 空时收起）。详见 `05` §3.1/§5 修订
M1-i  [x] 第二轮+第三轮验收反馈修复（2026-08-14/15）：弹窗居中改用具体类选择器（基类 `Screen` 被 Textual 8.2.8 覆盖）+ y/n 键与方向键选择 + 长命令参数截断/dialog 高度自适应（按钮不被挤出）；Todo/ToolRegion 初始 `display=False` 真正收起；clipboard 接系统剪贴板（pyperclip，Textual 默认进程内）；`ChatInput` 状态机过滤鼠标序列残留（完整 `\x1b[<...M`→MouseEvent，缺 ESC 的 `[<35;56;28m` 拆成逐字符 Key→状态机闭合判定整段丢弃）；Bash 输出剥离 ANSI（防 `\x1b` 污染上下文）；OpenAI 序列化 assistant `content` 空串替代 null + 捕获 400 响应体日志。**400 已确认解决**（完整多步工具调用 trace 跑通）。中文 IME 根因（用户环境修正）：**终端=conhost（cmd），未装 WT**——conhost 永不支持 Kitty，Textual 无条件 `\x1b[>1u` 在 conhost 下 no-op；**真正根因 = `enable_application_mode` 强制 `ENABLE_VIRTUAL_TERMINAL_INPUT`（win32.py:157），conhost 的 VT 输入对中文 IME 组合提交有缺陷**；Claude Code 不设 VT 输入、走传统 ReadConsoleInput 直读故中文正常。**对齐方案（M1-i2，用户选定「坚持 cmd」）**：`compat.py` monkeypatch ① 过滤 Kitty 序列；② enable_application_mode → 传统事件输入模式；③ EventMonitor.run 重写（VK 码→xterm 序列 + SGR 鼠标翻译）；另修输入框三项（backspace `\x08`→ctrl+h 兜底、ctrl+c/v priority、`_CONTROL_RE` 收窄）。pytest 149 全绿，待实测。详见 `05` §5 修订
```

**验收锚点**：`02` §5（能跑档 10 项）+ `03` §5（7 工具项）+ `04` §5 + `05` §5（能跑档项）。**核心 demo**：输入"写一个 HTTP 服务器并编译通过"，Agent 自主 Read/Grep/Write/Bash 闭环完成，全程 TUI 可见、会话可恢复。

**横切**：核心路径（消息/工具/会话）写 pytest 冒烟；TodoWrite 结构（todo→task→steps、判据可选）从 M1-f 就按 `12` T36 落地，为检查点打底。

### M2 能用（能用档）

**目标**：上下文三层压缩 + 结构化日志 + `/compact`——长会话不崩、出事能查。

**子任务**：

```
M2-a  01 L1 大结果落盘 + 读回闭环
M2-b  01 L2 在线摘要（经济性模型 + 信息密度门槛 + 落盘保留 D6）
M2-c  01 L3 Auto-Compact + 兜底（独立预算熔断/强制/紧急）+ 压缩后恢复（含 todo 快照重灌）
M2-d  07 Logging：统一 sink + span 落盘（traces/metrics JSONL）+ 脱敏
M2-e  05 /compact 命令（前后对比）+ 07 状态栏 token
```

**验收锚点**：`01` §11（8 项）+ `07` §5（日志/trace 落盘项）+ `04` §5（恢复触发压缩）。

**预研落地**：**DeepSeek 前缀缓存实测**——测缓存命中率、`C_hit` 折扣，回填 `01` T5-1/T7/T8 初步标定（L2 经济性参数、压缩阈值实测校准启动）。07 §3.6 标定基建随 M2-d 上线。

### M3 可控（可控档）

**目标**：权限五层 + Hook + trace/监控——危险被拦、行为可回溯。

**子任务**：

```
M3-a  06 权限：黑名单（Windows shell 模式集）+ 路径沙箱 + 规则引擎 + 四模式 + HITL（05 ModalScreen 复用）
M3-b  06 Hook：10 事件 + command/prompt/http 执行器 + pre_tool_use 拦截
M3-c  07 trace：埋点接通（llm.call/tool.exec/permission.check/hook.run/context.compact）+ span 树重建
M3-d  07 /metrics 面板（方案 A，T9）+ 05 /permissions 命令
M3-e  01 标定报表：从 M2 数据聚出 T7/T8/T5-1 校准表
```

**验收锚点**：`06` §5（11 项）+ `07` §5（trace/metrics 项）+ `05` §5（/permissions /metrics 项）。

**备注**：OS 级沙箱不做（D14）；M3-e 产出四待决项的首轮标定值 → 移入 00 §6.1。

### M4 好用（好用档）

**目标**：静默记忆 + MCP/Skill 生态——跨会话有记忆、工具生态开放。

**子任务**：

```
M4-a  08 静默读（MEMORY.md 索引注入）+ 静默写（双门槛节流 + 前缀缓存复用）+ 主动线提示词
M4-b  08 Dreaming 治理（门控/锁/四阶段）
M4-c  09 MCP：官方 SDK client + wrapper 注册 + 启动后台连接 + 延迟加载 + ToolSearch
M4-d  09 Skill：两阶段加载 + LoadSkill + inline 执行 + skill-creator（T24）+ /mcp /skills
M4-e  05 /memory 命令；Skill 自动注册 Slash 命令
```

**验收锚点**：`08` §5（10 项）+ `09` §5（12 项）+ `05` §5（/memory 项）。

**预研落地**：官方 `mcp` 包接入验证（stdio + Streamable HTTP 双 transport）；Skill frontmatter 解析复用。

### M5 生产级（生产级档）

**目标**：SubAgent/Worktree + 评估 + Harness——可量化、可隔离并行、高可靠。

**子任务**：

```
M5-a  10 Agent≈Tool + Task 工具 + 后台模式 + 工具过滤四层 + 内置 4 Agent
M5-b  10 Git Worktree（生命周期/变更保护/过期清理）+ isolation: worktree
M5-c  11 评估 driver：SWE-bench-Live 流水线（封史/gold/判分）+ WSL2 Docker 环境 + 失败归类 + 报告屏
M5-d  12 测试闭环：TestRunner 工具（worktree 沙箱 + TestingEvent）+ 归因定向重试 + Fail2Pass/Pass2Pass
M5-e  12 双层检查点 + todo 4 时点刷新 + Verification 子 Agent + 断路器
M5-f  12 错误模式沉淀（08 feedback）+ 规则量化追踪（07 span 聚合）
M5-g  12→11 迭代闭环：一次一变量复测 + diff_runs 对比
```

**验收锚点**：`10` §5（13 项）+ `11` §5（13 项）+ `12` §5（19 项）。

**预研落地**：WSL2 + Docker SWE-bench 判分环境搭建（国内镜像加速）；git worktree Windows 兼容（软链 best-effort、GIT_TERMINAL_PROMPT=0）。

---

## 4. 关键技术预研点（前置决策）

| 预研点 | 影响 | 需在何时前 | 备注 |
|---|---|---|---|
| DeepSeek 前缀缓存实测（命中率/`C_hit`） | `01` T5-1/T7/T8 标定 | M2 | 影响 L2 经济性决策 |
| Textual Windows 渲染（中文/滚动/ModalScreen） | `05` | M1-e | 开发机直测 |
| 官方 `mcp` 包双 transport | `09` T20 | M4-c | stdio 先、HTTP 后 |
| WSL2 + Docker SWE-bench 判分环境 | `11` §3.3 方案 A | M5-c | 首轮镜像拉取约 10 分钟/题 |
| git worktree Windows 兼容 | `10` §3.13 | M5-b | 软链 best-effort |

---

## 5. 验收机制

- **里程碑完成 = 锚定规格 §5 勾选全绿 + 可演示**（手动 demo + pytest 冒烟）。
- 规格 §5 是唯一完成判据；与 `12` 的"完成 = 判据被满足"同一把尺子。
- 跨里程碑改进（prompt/超参/harness）一律走 `11` 一次一变量复测（M5-g），M1-M4 阶段用 pytest 回归 + demo 兜底。
- 每里程碑结束回写 `00` 进度跟踪，勾选即更新，保持上下文可接续。

---

## 6. TO-DECIDE

| # | 待决项 | 归属 | 建议 |
|---|---|---|---|
| I1 | 包名 | M0 | `kdagent`（D7 产品名对齐） |
| I2 | 依赖管理 | M0 | `uv` + `uv.lock`（快、锁文件） |
| I3 | Python 版本 | M0 | ≥3.11，开发机实测为准 |
| I4 | M1 内部并行范围 | M1 | 01/02 先行，03/04 并行，05 后置 |
| I5 | 首个里程碑要不要先做"DeepSeek 直连 demo"再铺骨架 | M1 | 建议先 1 天验证 provider 连通性，再铺 M1-a |

> 均非设计决策，M0 阶段确认即可，不进入 00 §6。
