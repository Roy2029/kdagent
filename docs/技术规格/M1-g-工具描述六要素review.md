# M1-g 工具描述六要素 Review（人工）

> **里程碑**：M1-g（`13` 子任务卡）｜**判据**：`03` §5「内置工具描述遵循六要素规范（人工 code review 审查，非自动化）」= [03 §3.3](03-工具系统.md) 六要素
> **方法**：D11 —— 不做自动检查，人工逐工具核对。本文件为核对底稿；**直接在本文改**，确认后我把改动同步回代码。
> **范围**：7 个内置工具（`src/kdagent/tools/{filesystem,shell,todo}.py`）的 `description` 字段（LLM 实际看到的提示文本）+ `input_schema` 的字段说明。

---

## 1. 六要素回顾（03 §3.3）

| 要素 | 说明 |
|---|---|
| 做什么 | 核心功能 |
| 何时该用 | 典型场景 |
| 何时不该用 | 反模式 |
| 参数约束 | 输入限制 |
| 返回格式 | 输出形态 |
| 与其他工具配合 | 工作流建议 |

---

## 2. 覆盖总览

| 工具 | 做什么 | 何时该用 | 何时不该用 | 参数约束 | 返回格式 | 配合 | 结论 |
|---|---|---|---|---|---|---|---|
| ReadFile | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | 通过 |
| WriteFile | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | 通过 |
| EditFile | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | 通过（含唯一匹配说明） |
| Glob | ✅ | ✅ | ✅ | ✅ | ✅ | ❌ 缺 | **需补"配合"** |
| Grep | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | 通过 |
| Bash | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | 通过 |
| TodoWrite | ✅ | ⚠️ 隐含无标签 | ❌ 缺 | ✅ | ✅ | ❌ 缺 | **需补"何时不该用"+"配合"** |

**共性问题**：六要素全部用连续字符串拼接进 `description`，靠「何时使用 / 何时不使用 / 参数约束 / 返回格式 / 配合」中文标签分隔。五个文件工具已形成统一标签风格；**Glob / TodoWrite 未跟上该风格**（缺"配合"标签；TodoWrite 连"何时"标签都无）。

---

## 3. 逐工具 Review

### 3.1 ReadFile ✅

**当前 description**（`filesystem.py:42-49`）：

```python
description = (
    "读取指定文件的文本内容，返回带行号文本。"
    "何时使用：需要查看文件完整内容或特定行范围时。"
    "何时不使用：二进制文件不可读（请用 Bash）；只需定位关键词请用 Grep。"
    "参数约束：path 必须是绝对路径；大文件建议先 Grep 定位再用 offset/limit 读取片段。"
    "返回格式：每行带行号前缀；文件不存在返回错误。"
    "配合：定位用 Grep → 读片段用 ReadFile(offset/limit) → 改动用 EditFile。"
)
```

**六要素拆解**：全覆盖，质量好。offset/limit 的用法建议直接内嵌在描述里，LLM 无需猜。

**可商榷点**（待你决定，默认不改）：
- 「二进制文件不可读（请用 Bash）」——Bash 读二进制同样输出乱码，更准确的说法是「二进制文件请勿用 ReadFile 直读；确需检查用 Bash 的 `file` 命令」。M1 档位下两者皆可，倾向于保留现状（低优先）。

---

### 3.2 WriteFile ✅

**当前 description**（`filesystem.py:105-112`）：

```python
description = (
    "创建新文件或完全覆写现有文件，返回写入统计。"
    "何时使用：需要创建新文件或全文重写时。"
    "何时不使用：局部修改请用 EditFile（精确替换，不破坏其余内容）。"
    "参数约束：path 必须是绝对路径；content 为完整文件内容（可为空串）。"
    "返回格式：写入字符数与路径。"
    "配合：先 ReadFile 确认现状再覆写。"
)
```

**六要素拆解**：全覆盖，通过。

---

### 3.3 EditFile ✅

**当前 description**（`filesystem.py:160-168`）：

```python
description = (
    "在指定文件中精确替换 old_string 为 new_string，返回替换统计。"
    "old_string 必须在文件中唯一匹配；出现多次会报错（避免误改）。"
    "何时使用：局部修改文件内容时。"
    "何时不使用：全文重写用 WriteFile；先确认现状请 ReadFile。"
    "参数约束：path 必须是绝对路径；old_string 唯一，多匹配报错。"
    "返回格式：替换结果与匹配计数。"
    "配合：先 Grep 定位 old_string 确保唯一。"
)
```

**六要素拆解**：全覆盖，通过。首行「唯一匹配 / 多匹配报错」提前点出最易触发的失败模式，是好的描述。

---

### 3.4 Glob ⚠️ 缺「配合」

**当前 description**（`filesystem.py:229-235`）：

```python
description = (
    "按路径模式匹配项目文件，返回匹配的相对路径列表。"
    "何时使用：探索项目结构、查找某类文件时。"
    "何时不使用：按内容搜索用 Grep。"
    "参数约束：pattern 支持 ** 递归；path 为搜索基准目录（默认工作目录）。"
    "返回格式：每行一个匹配路径（相对基准目录）。"
)
```

**缺失**：无「配合」要素；且未声明 `path` 支持相对 `work_dir`（实现 `filesystem.py:266` 用 `_absolute(ctx.work_dir, raw_path)`，相对/绝对皆可）与「无匹配返回空」。

**建议改后**（可直接替换，新增 2 处）：

```python
description = (
    "按路径模式匹配项目文件，返回匹配的相对路径列表。"
    "何时使用：探索项目结构、查找某类文件时。"
    "何时不使用：按内容搜索用 Grep。"
    "参数约束：pattern 支持 ** 递归；path 为搜索基准目录（默认工作目录），相对/绝对皆可。"
    "返回格式：每行一个匹配路径（相对基准目录）；无匹配返回空。"
    "配合：先 Glob 摸清文件布局 → Grep 定位具体内容 → ReadFile 读取片段。"
)
```

---

### 3.5 Grep ✅

**当前 description**（`filesystem.py:280-287`）：

```python
description = (
    "用 ripgrep 按正则表达式搜索文件内容，返回匹配行。"
    "何时使用：在项目中定位关键词、模式时。"
    "何时不使用：按文件名查找用 Glob。"
    "参数约束：pattern 为正则；path 为搜索基准目录（默认工作目录）；glob 过滤文件类型。"
    "返回格式：path:行号:内容，每行一个匹配；无匹配返回空（非错误）。"
    "配合：定位到行后用 ReadFile 读上下文。"
)
```

**六要素拆解**：全覆盖，通过。特别点出「无匹配返回空（非错误）」避免 LLM 把正常空结果当失败，很好。

**可商榷点**（待你决定，默认不改）：
- 未声明「依赖 rg（ripgrep）需已安装」。M1 环境已装 rg 15.1.0，可提一句「依赖 ripgrep（rg）」让描述诚实；当前实现缺失时会返回错误。倾向补，低优先。

---

### 3.6 Bash ✅

**当前 description**（`shell.py:21-28`）：

```python
description = (
    "执行 shell 命令，返回 stdout、stderr 与退出码。"
    "何时使用：需要运行编译器、脚本、构建、文件操作命令时。"
    "何时不使用：纯文件读取/搜索请用 ReadFile/Grep（更安全、无副作用）。"
    "参数约束：command 为待执行命令；优先 bash（Git Bash），无则回退系统 shell。"
    "返回格式：stdout + stderr + [exit] 退出码；退出码非 0 视为错误。"
    "配合：写代码 → Bash 编译验证。"
)
```

**六要素拆解**：全覆盖，通过。

**可商榷点**（待你决定，默认不改）：
- 未声明执行工作目录 = `work_dir`（`shell.py:64` `cwd=str(ctx.work_dir)`）。补「命令在工作目录执行」可减少 LLM 对相对路径的误用。倾向补，低优先。

---

### 3.7 TodoWrite ⚠️ 缺「何时不该用」+「配合」

**当前 description**（`todo.py:69-73`）：

```python
description = (
    "写入或更新任务规划列表。结构为 todo → task → steps："
    "todo 是整体目标，task 是可执行子任务，step 是任务的具体步骤"
    "（每步可带可选完成判据 accept_criteria）。任务推进时用 TodoWrite 更新状态并回传完整列表。"
)
```

**缺失**：
- 无「何时不该用」——未防止 LLM 对一次性小请求滥用规划。
- 无「配合」——未衔接 todo 面板 / 检查点（12 T35）与判据规范（12 T36）。
- 「何时该用」有内容但无标签（「任务推进时更新状态」），弱化格式一致性。

**顺带发现（代码层，非描述）**：`normalize_todos` 只接受 `status ∈ {pending, completed}`（`todo.py:42-44`），但 `TodoItemRecord.status` 注释写 `pending / in_progress / completed`（`records.py:55`）——**两者不一致**。in_progress 目前会被归成 pending。是否补 in_progress 属于数据模型决策，本文档先标注，不回改代码。

**建议改后**（可直接替换）：

```python
description = (
    "写入或更新任务规划列表。结构为 todo → task → steps：todo 是整体目标，"
    "task 是可执行子任务，step 是任务的具体步骤（每步可带可选完成判据 accept_criteria）。"
    "何时使用：接到多步任务时先规划 todo，任务推进时更新状态并回传完整列表。"
    "何时不使用：一次性小请求无需规划；不要为单一操作创建 todo。"
    "参数约束：每次调用回传全部 todo（非增量）；status 仅 pending/completed；空 content 项会被忽略。"
    "返回格式：回传完整规范化快照（含每任务状态与判据）。"
    "配合：todo 状态驱动 UI 面板与检查点（12 T35）；完成型步骤必写判据、探索型可省（12 T36）。"
)
```

---

## 4. 待你确认的汇总

| # | 项 | 我的建议 | 结论（2026-08-14 用户确认） |
|---|---|---|---|
| 1 | **Glob** 补「配合」+ 声明 path 相对/绝对皆可 + 无匹配返回空 | 采纳建议描述 | ✅ 改（`filesystem.py:229-235`） |
| 2 | **TodoWrite** 补「何时不该用」+「配合」+ 标签化「何时该用」 | 采纳建议描述 | ✅ 改（`todo.py:69-77`） |
| 3 | ReadFile 「二进制请用 Bash」表述 | 不改（低优先） | ❌ 不改 |
| 4 | Grep 补「依赖 ripgrep」 | 可改可不改（低优先） | ❌ 不改 |
| 5 | Bash 补「命令在 work_dir 执行」 | 可改可不改（低优先） | ✅ 改（`shell.py:21-29`） |
| 6 | **in_progress 状态缺口**（normalize 只收 pending/completed，与 records.py 注释不一致） | 属数据模型决策，本轮不回改 | ❌ 本轮不改（后续里程碑再议） |

> ✅ M1-g 完成（2026-08-14）：1/2/5 同步进代码；pytest 136 passed / ruff clean / mypy strict；`03` §5 已勾选；`13`/`00` 已回写。
